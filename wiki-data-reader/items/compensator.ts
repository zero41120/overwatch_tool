import type { ItemRecord } from "../types";

const record: ItemRecord = {
  item: {
    name: "COMPENSATOR",
    attributes: [
      {
        type: "WP",
        value: "5%"
      }
    ],
    cost: 1000,
    id: "i_0",
    tab: "weapon",
    rarity: "common"
  }
};

export default record;
