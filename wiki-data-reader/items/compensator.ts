import type { ItemRecord } from "../types";

const record: ItemRecord = {
  item: {
    name: "COMPENSATOR",
    attributes: [
      {
        type: "WP",
        value: "5%"
      }
    ],
    cost: 1000,
    tab: "weapon",
    rarity: "common",
    id: "i_0"
  }
};

export default record;
